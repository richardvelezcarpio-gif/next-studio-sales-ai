import type { Language, Lead } from '../../types';
export type Priority='critical'|'high'|'medium'|'low'; export interface LeadIntelligence { score:number; priority:Priority; reason:string } export interface Recommendation { action:'whatsapp'|'call'|'landing'|'card'|'appointment'|'none'; title:string; reason:string } export interface MessageRequest { lead:Lead; channel:string; purpose:string; tone:string; language:Language; company:string } export interface ObjectionResponse { response:string; strategy:string } export interface SalesAIProvider { calculateLeadScore(lead:Lead,interactions:number,overdue:boolean):LeadIntelligence; recommendNextAction(lead:Lead,intelligence:LeadIntelligence,overdue:boolean):Recommendation; generateMessage(request:MessageRequest):string; handleObjection(objection:string,language:Language):ObjectionResponse }
