import {supabase} from '../../lib/supabase';
export const tasksRepository={getAll:async()=>{if(!supabase)throw Error('Cloud not configured');return supabase.from('tasks').select('*').order('due_at')},create:async(data:Record<string,unknown>)=>{if(!supabase)throw Error('Cloud not configured');return supabase.from('tasks').insert(data).select().single()},update:async(id:string,data:Record<string,unknown>)=>{if(!supabase)throw Error('Cloud not configured');return supabase.from('tasks').update(data).eq('id',id).select().single()},remove:async(id:string)=>{if(!supabase)throw Error('Cloud not configured');return supabase.from('tasks').delete().eq('id',id)}};
