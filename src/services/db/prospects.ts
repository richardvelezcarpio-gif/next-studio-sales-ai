import {supabase} from '../../lib/supabase';
export const prospectsRepository={getAll:async()=>{if(!supabase)throw Error('Cloud not configured');return supabase.from('prospects').select('*').order('created_at',{ascending:false})},getById:async(id:string)=>{if(!supabase)throw Error('Cloud not configured');return supabase.from('prospects').select('*').eq('id',id).single()},create:async(data:Record<string,unknown>)=>{if(!supabase)throw Error('Cloud not configured');return supabase.from('prospects').insert(data).select().single()},update:async(id:string,data:Record<string,unknown>)=>{if(!supabase)throw Error('Cloud not configured');return supabase.from('prospects').update(data).eq('id',id).select().single()},remove:async(id:string)=>{if(!supabase)throw Error('Cloud not configured');return supabase.from('prospects').delete().eq('id',id)}};
